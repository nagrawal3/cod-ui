import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles(theme => ({
  label: {
    fontWeight: 'bold',
  },
  item: {
    flex: 'none',
    minWidth: '11rem',
  },
}));

export default useStyles;
